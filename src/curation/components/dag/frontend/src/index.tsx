import React from 'react'
import ReactDOM from 'react-dom/client'
import CurationDag from './CurationDag'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <CurationDag />
  </React.StrictMode>,
)
